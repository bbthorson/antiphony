{
  "name": "@antiphony/audio-rendition",
  "version": "0.1.0",
  "private": true,
  "description": "Derives renditions of Antiphony audio blobs (mp3 today) and writes them to R2. The one Antiphony service that is not a Worker: it shells out to ffmpeg, which a Workers runtime cannot do. See specs/archive/cloudflare-migration.md § The ffmpeg problem.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "//dev": "Needs a local ffmpeg. The startup probe says whether you have one.",
    "dev": "tsx watch src/index.ts",
    "build": "node esbuild.config.mjs",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.0",
    "aws4fetch": "^1.0.20",
    "hono": "^4.12.22"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "eslint": "^9.39.2",
    "globals": "^15.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=22"
  }
}
